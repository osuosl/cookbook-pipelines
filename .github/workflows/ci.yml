---
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ruby/setup-ruby@v1
        with:
          # Matches the cinc-workstation omnibus ruby the pipelines run on.
          ruby-version: '3.4'
          bundler-cache: true
      - name: Rubocop
        run: bundle exec rake style
      - name: RSpec
        run: bundle exec rake spec
